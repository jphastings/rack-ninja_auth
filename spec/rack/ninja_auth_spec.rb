require 'spec_helper'

describe Rack::NinjaAuth do
  it 'has a version number' do
    expect(Rack::NinjaAuth::VERSION).not_to be nil
  end

  it 'has some tests' do
    expect(false).to eq(true)
  end
end
